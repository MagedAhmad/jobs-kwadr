@can('update', $training_type)
    <a href="{{ route('dashboard.training_types.edit', $training_type) }}" class="btn btn-outline-primary btn-sm">
        <i class="fas fa fa-fw fa-edit"></i>
    </a>
@endcan
